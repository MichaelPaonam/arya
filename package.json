{
  "name": "arya",
  "private": true,
  "scripts": {
    "build": "cd packages/frontend && npm run build",
    "install:all": "cd packages/frontend && npm install --legacy-peer-deps && cd ../agents && npm install"
  },
  "dependencies": {
    "next": "14.2.35"
  }
}
